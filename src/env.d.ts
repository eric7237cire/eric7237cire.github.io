declare var process: {
  env: {
    NG_APP_ENV: string;
    NG_APP_COMMITTED_AT: string;
    NG_APP_GITHUB_SHA: string;
    NG_APP_GITHUB_REF_NAME: string;

    // Replace the line below with your environment variable for better type checking
    //[key: string]: any;
  };
};
